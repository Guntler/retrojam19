using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//TODO:
//Save a reference to what the previous state was so that every single thing that subscribes to it doesn't get picked up
public class BlackOverlayUpdateCompleteEvent : GameEvent
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
